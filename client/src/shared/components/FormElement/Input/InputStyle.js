import styled from "styled-components";
import { rem } from 'polished';
import { Input } from '../../../style/globalInputs';

export const AuthInput = styled(Input)`
 
  border-bottom: ${rem("1px")} solid rgba(149,149,149,0.90);
`;